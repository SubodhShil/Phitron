#include <bits/stdc++.h>
using namespace std;

int main()
{
    string str;
    cin >> str;

    stack<char> st;

    for (auto i : str)
    {
        if (i >= '1' && i <= '9')
        {
            int x = i - '0';
        }
    }
    return 0;
}