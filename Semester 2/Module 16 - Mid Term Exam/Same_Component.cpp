#include <bits/stdc++.h>
using namespace std;

vector<string> graph;
int n, m;

int main()
{
    cin >> n >> m;
    for (int i = 0; i < n; ++i)
    {
        string s;
        cin >> s;
        graph.push_back(s);
    }

    int si, sj, di, dj;
    cin >> si >> sj >> di >> dj;

    return 0;
}